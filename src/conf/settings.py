USE_I18N = True

# Valid languages
LANGUAGES = (
    # 'en', 'zh_TW' should match the directories in conf/locale/*
    ('en', _('English')),  
    )

# This is a default language
LANGUAGE_CODE = 'en'

# Custom template tags
INSTALLED_APPS = (
    'lso',
    )
